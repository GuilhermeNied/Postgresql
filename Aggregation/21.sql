select	fac.name as name, fac.initialoutlay/((sum(case when memid = 0 then slots * fac.guestcost else slots * membercost end)/3) - fac.monthlymaintenance) as months from cd.bookings bk inner join cd.facilities fac on bk.facid = fac.facid group by fac.facid order by name